{
  "name": "comic-api",
  "version": "1.0.0",
  "workspaces": [
    "comic-front",
    "comic-server"
  ],
  "scripts": {
    "install": "npm install",
    "start": "concurrently \"npm:start:server\" \"npm:start:frontend\"",
    "start:server": "npm --prefix comic-server start",
    "start:frontend": "npm --prefix comic-front start"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
